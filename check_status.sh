#!/bin/bash
# =============================================================================
# check_status.sh - システムリソースとプロセスの監視
# =============================================================================

watch -n 1 'free -h && echo && ps aux | grep -E "python|node" | head -15'
